name: oc_admin_app
description: "OpenClaw Binding & Management Client"
publish_to: 'none'
version: 1.0.3+5

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  pocketbase: ^0.23.2
  provider: ^6.1.2
  go_router: ^14.2.7
  flutter_secure_storage: ^9.2.2
  intl: ^0.19.0
  fl_chart: ^0.69.0
  shimmer: ^3.0.0
  pull_to_refresh: ^2.0.0
  shared_preferences: ^2.2.0
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
